{{ form_start(form_delete, {'action': path('app_publication_delete', {'id': publication.id}), 'method': 'DELETE', 'attr': {'class': 'd-inline', 'onsubmit': 'return confirm("Are you sure you want to delete this publication? This action cannot be undone.");'}}) }}
    <button class="btn btn-danger">
        <i class="fas fa-trash-alt me-1"></i> Delete
    </button>
{{ form_end(form_delete) }}